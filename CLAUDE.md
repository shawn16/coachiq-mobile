# CoachIQ Development Rules

## Security
- Never hardcode credentials, API keys, connection strings, or secrets in source code
- Always use environment variables for sensitive values
- Never commit .env files to Git

## Git
- Use git config user.email "your-email@yahoo.com" for all commits
- Use git config user.name "Shawn Siemers" for all commits
EOF